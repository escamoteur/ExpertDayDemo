<?xml version="1.0" encoding="utf-8" ?>
<xamForms:ReactiveContentPage x:TypeArguments="local:MainPageModel" xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ExpertDayDemo"
             xmlns:xamForms="clr-namespace:ReactiveUI.XamForms;assembly=ReactiveUI.XamForms"
             x:Class="ExpertDayDemo.MainPage" BackgroundColor="LightBlue">
    <ContentPage.Content>
        <StackLayout>
            <Entry x:Name="SearchText" VerticalOptions="Start"/>
            <ListView x:Name="CityListView" ItemsSource="{Binding CityWeatherList}" VerticalOptions="StartAndExpand">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <local:CityWeatherItemView />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button x:Name="UpdateBtn" Text="Update" VerticalOptions="End"/>
        </StackLayout>
    </ContentPage.Content>
</xamForms:ReactiveContentPage>